splashUI(type, option1, option2 = "", option3 = ""){
	if type = t
		splashText(option1, option2, option3)
	if type = r
		splashRadio(option1, option2)
	if type = r2
		splashRadio2(option1, option2)
	if type = p
		splashProgress(option1)
}